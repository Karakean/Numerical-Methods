function [x_aprox] = approx_trig(n,x,N)

n = n*(pi/max(n));
S = zeros(N+1,N+1);

M=size(n,2);
for i=1:M
    for j=1:M
        S(i,j) = sum(cos((i-1).*n).*cos((j-1).*n));
    end
end

t = zeros(N+1,1);
for k = 1:N+1
    t(k,1) = sum(x.*cos((k-1)*n));
end


% Solving equation Sc = t
%...
%...

c1 = cos((0:N)'*n);

x_aprox = (c1' * c).';

end
